# Rules

- Never read `.env`, `.env.local`, or any file containing secrets/API keys (e.g. matching `.env*`, `*secret*`, `*credentials*`). Do not open, cat, or print their contents under any circumstance, even if asked directly.
